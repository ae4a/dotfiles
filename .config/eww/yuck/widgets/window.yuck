(deflisten get_active_window :initial "" `scripts/active_window.sh`)

(defwidget active_window []
  (box
    :class "active_window"
    (label
      :truncate true
      :limit-width 100
      :text {get_active_window == "null" ? "" : get_active_window}
    )
    
  )
)
